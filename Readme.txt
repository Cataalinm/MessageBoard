The interface of the application was made as a WPF application with a ClassLibrary added for the 2 classes that were used

An account with the username "admin" and password "admin" is provided by default